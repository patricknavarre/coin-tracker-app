import React from 'react'
import "./Spinner.css";

function Spinner() {
    return (
        <div className="loader" style={{marginTop: "15%" }}>
        Loading...
        </div>
    )
}

export default Spinner
